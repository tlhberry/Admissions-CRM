─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────–─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────–# deploy/.env.template  –  copy to .env, fill values, never commit .env

# Database
DATABASE_URL=postgresql://admitsimple:CHANGE_ME@db:5432/admitsimple
POSTGRES_DB=admitsimple
POSTGRES_USER=admitsimple
POSTGRES_PASSWORD=CHANGE_ME

# App
NODE_ENV=production
PORT=5000
SESSION_SECRET=CHANGE_ME_64_CHARS_MIN

# AI
ANTHROPIC_API_KEY=sk-ant-CHANGE_ME

# AWS S3
AWS_ACCESS_KEY_ID=CHANGE_ME
AWS_SECRET_ACCESS_KEY=CHANGE_ME
AWS_REGION=us-east-1
AWS_S3_BUCKET=CHANGE_ME

# Authorize.net
AUTHNET_API_LOGIN_ID=CHANGE_ME
AUTHNET_TRANSACTION_KEY=CHANGE_ME
AUTHNET_ENVIRONMENT=sandbox

# SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=CHANGE_ME
SMTP_PASS=CHANGE_ME
SMTP_FROM=noreply@yourdomain.com

# CTM
CTM_WEBHOOK_TOKEN=CHANGE_ME
CTM_WEBHOOK_SECRET=CHANGE_ME

# Domain (used by nginx + certbot)
DOMAIN=app.yourdomain.com
